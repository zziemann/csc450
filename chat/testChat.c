#include <stdio.h>
#include <string.h>

int main(int argc, char** argv)
{
    // /*
    //Showing how to cut off the beginning, also comparing 
    int maxSize = 1000;
    char* s = "lol msg 4 hello";
    char* s2 = "world";
    int compare = strcmp("wound", s2);
    printf("here:%i", compare);
    //puts(strstr(s, "msg"));
    //printf("%d\n", val);
    // */
    
    
    /*
    //testing gets and accepting strings from terminal 
    char str[100];
    puts("Enter your message: ");
    gets(str);
    
    printf("You just sent out this message: %s\n", str);
    
    */
}